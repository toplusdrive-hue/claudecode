# NOTE: This file must stay ASCII-only.
# pip reads requirements.txt with the system locale codec (cp949 on Korean
# Windows), so non-ASCII bytes here crash the install with UnicodeDecodeError.
# Korean notes for this file live in README.md, not here.
#
# Verified combination: Python 3.11 / CapCut 9.3.0.3969 (beta) / Windows 11
# torch is NOT required (~2.5GB saved): faster-whisper runs Silero VAD
# through onnxruntime.

fastapi
uvicorn[standard]
python-multipart

pycapcut==0.0.3
faster-whisper==1.2.1

rapidfuzz
psutil

# Pulled in by pycapcut; pinned here so a missing wheel is obvious.
pymediainfo
imageio

# Only needed for the auto-export step (4th stage). Windows only.
uiautomation>=2 ; sys_platform == "win32"
